apply plugin: 'java'
apply plugin:'application'

mainClassName = "sorter.SortFile"
jar {
    manifest {
        attributes 'Main-Class': 'sorter.SortFile'
    }
}

repositories {
    jcenter()
}

dependencies {
  testCompile 'junit:junit:4.12'
}

